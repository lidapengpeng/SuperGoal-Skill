interface:
  display_name: "SuperGoal"
  short_description: "Loop-engineered controller: repo recon, grounded clarifying questions, no silent assumptions, one Agree checkpoint, DAOR experiment cycles, adversarial review."
  default_prompt: "Use $supergoal to run this task through its full workflow: quick recon, grounded clarification with an assumption ledger, one Agree checkpoint, Design-Act-Observe-Reason cycles with a run ledger, adversarial review gates, and an evidence-based completion audit."

policy:
  # Heavyweight workflow: only run when explicitly invoked with $supergoal.
  allow_implicit_invocation: false
